# Tiflis Code Workstation Configuration
# Copy this file to .env and customize

# ─────────────────────────────────────────────────────────────
# Tunnel Connection (Required)
# ─────────────────────────────────────────────────────────────
# WebSocket URL of your tunnel server
TUNNEL_URL=wss://tunnel.example.com/ws

# API key (must match TUNNEL_REGISTRATION_API_KEY on tunnel server)
TUNNEL_API_KEY=

# ─────────────────────────────────────────────────────────────
# Workstation Settings (Required)
# ─────────────────────────────────────────────────────────────
# Auth key for mobile clients (min 16 chars)
# Generate with: openssl rand -base64 24 | tr -dc 'a-zA-Z0-9' | head -c 24
WORKSTATION_AUTH_KEY=

# Display name for this workstation
WORKSTATION_NAME=

# Root directory containing workspace folders
WORKSPACES_ROOT=/path/to/workspaces

# Data directory for database and cache
DATA_DIR=~/.tiflis-code/workstation/data

# ─────────────────────────────────────────────────────────────
# Server Settings
# ─────────────────────────────────────────────────────────────
PORT=3002
HOST=127.0.0.1
NODE_ENV=production
LOG_LEVEL=info

# ─────────────────────────────────────────────────────────────
# AI Agent (Optional)
# ─────────────────────────────────────────────────────────────
# Provider: openai, anthropic, cerebras, groq, together
# AGENT_PROVIDER=openai
# AGENT_API_KEY=
# AGENT_MODEL_NAME=gpt-4o-mini
# AGENT_BASE_URL=  # Optional: custom API endpoint
# AGENT_TEMPERATURE=0

# ─────────────────────────────────────────────────────────────
# Speech Services (Optional)
# ─────────────────────────────────────────────────────────────
# Speech-to-Text
# STT_PROVIDER=openai
# STT_API_KEY=
# STT_MODEL=whisper-1
# STT_LANGUAGE=en

# Text-to-Speech
# TTS_PROVIDER=openai
# TTS_API_KEY=
# TTS_MODEL=tts-1
# TTS_VOICE=alloy

# ─────────────────────────────────────────────────────────────
# Agent Aliases (Optional)
# ─────────────────────────────────────────────────────────────
# Define custom agent commands
# AGENT_ALIAS_ZAI=claude --settings /path/to/settings.json
# AGENT_ALIAS_CODER=cursor-agent -p

# ─────────────────────────────────────────────────────────────
# Advanced Settings
# ─────────────────────────────────────────────────────────────
# AGENT_EXECUTION_TIMEOUT=900
# TERMINAL_OUTPUT_BUFFER_SIZE=100
