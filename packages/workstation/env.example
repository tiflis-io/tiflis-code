# Workstation Server Environment Configuration
# Copy this file to .env.local and fill in the values

# ─────────────────────────────────────────────────────────────
# Server Configuration
# ─────────────────────────────────────────────────────────────

# Environment: development | production | test
NODE_ENV=development

# HTTP server port (for local health checks)
PORT=3002

# Bind address
HOST=0.0.0.0

# Logging level: trace | debug | info | warn | error | fatal
LOG_LEVEL=info

# ─────────────────────────────────────────────────────────────
# Tunnel Connection
# ─────────────────────────────────────────────────────────────

# Tunnel server WebSocket URL
# Example: wss://tunnel.example.com/ws or ws://localhost:3001/ws
TUNNEL_URL=ws://localhost:3001/ws

# API key for tunnel registration (must match TUNNEL_REGISTRATION_API_KEY on tunnel server)
TUNNEL_API_KEY=your-32-character-api-key-here!!!

# ─────────────────────────────────────────────────────────────
# Workstation Configuration
# ─────────────────────────────────────────────────────────────

# Display name for this workstation (shown to mobile clients)
WORKSTATION_NAME="My Workstation"

# Auth key for mobile client authorization (min 16 characters)
# This key should be shared with mobile app via QR code
WORKSTATION_AUTH_KEY=your-auth-key-here

# ─────────────────────────────────────────────────────────────
# Workspaces Configuration
# ─────────────────────────────────────────────────────────────

# Root directory containing workspaces (organizations/groups)
# Each subdirectory is a workspace, containing projects (git repos)
WORKSPACES_ROOT=/Users/yourname/work

# ─────────────────────────────────────────────────────────────
# Data Storage
# ─────────────────────────────────────────────────────────────

# Base directory for data storage (database, audio files, logs)
# Defaults to ~/.tiflis-code if not specified
DATA_DIR=

# ─────────────────────────────────────────────────────────────
# Supervisor Agent (LLM) Configuration
# ─────────────────────────────────────────────────────────────

# LLM Provider: openai | cerebras | anthropic | groq | together
AGENT_PROVIDER=openai
AGENT_API_KEY=sk-your-api-key-here

# Model name (provider-specific)
# OpenAI: gpt-4o-mini, gpt-4o, gpt-4-turbo
# Cerebras: llama3.1-8b, llama3.1-70b (fast inference!)
# Anthropic: claude-3-5-sonnet-20241022
AGENT_MODEL_NAME=gpt-4o-mini

# Base URL for OpenAI-compatible APIs (required for Cerebras, optional for others)
# Cerebras: https://api.cerebras.ai/v1
# Together: https://api.together.xyz/v1
# Groq: https://api.groq.com/openai/v1
# AGENT_BASE_URL=

# Temperature (0.0 - 1.0), lower = more deterministic
AGENT_TEMPERATURE=0

# ── Example: Cerebras (fast inference) ──
# AGENT_PROVIDER=cerebras
# AGENT_API_KEY=your-cerebras-api-key
# AGENT_MODEL_NAME=llama3.1-70b
# AGENT_BASE_URL=https://api.cerebras.ai/v1

# ─────────────────────────────────────────────────────────────
# Speech-to-Text (STT) Configuration
# ─────────────────────────────────────────────────────────────

# STT Provider: openai | elevenlabs | deepgram
STT_PROVIDER=openai
STT_API_KEY=sk-your-api-key-here

# Model name
# OpenAI: whisper-1
# Deepgram: nova-2
STT_MODEL=whisper-1

# Base URL (optional, for self-hosted or proxied APIs)
# STT_BASE_URL=

# Default language for speech recognition
STT_LANGUAGE=en

# ─────────────────────────────────────────────────────────────
# Text-to-Speech (TTS) Configuration
# ─────────────────────────────────────────────────────────────

# TTS Provider: openai | elevenlabs
TTS_PROVIDER=openai
TTS_API_KEY=sk-your-api-key-here

# Model name
# OpenAI: tts-1 (fast), tts-1-hd (high quality)
# ElevenLabs: eleven_flash_v2_5 (fast), eleven_multilingual_v2 (quality)
TTS_MODEL=tts-1

# Voice ID
# OpenAI: alloy, echo, fable, onyx, nova, shimmer
# ElevenLabs: voice ID from your library (e.g., uYXf8XasLslADfZ2MB4u)
TTS_VOICE=alloy

# Base URL (optional)
# TTS_BASE_URL=

# ── Example: ElevenLabs (high quality TTS) ──
# TTS_PROVIDER=elevenlabs
# TTS_API_KEY=your-elevenlabs-api-key
# TTS_MODEL=eleven_flash_v2_5
# TTS_VOICE=uYXf8XasLslADfZ2MB4u

# ─────────────────────────────────────────────────────────────
# Headless Agents Configuration
# ─────────────────────────────────────────────────────────────

# Timeout for headless agent command execution (seconds)
# Default: 900 (15 minutes) - agents may need time for complex tasks
AGENT_EXECUTION_TIMEOUT=900

# Wait time after Claude CLI process termination before next command (ms)
# Claude CLI needs time to release session lock
CLAUDE_SESSION_LOCK_WAIT_MS=1500

# OpenCode daemon URL (if using opencode agent)
# OPENCODE_DAEMON_URL=http://localhost:4200

