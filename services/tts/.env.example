# =============================================================================
# Tiflis Code TTS API Configuration
# =============================================================================
# Copy this file to .env and modify as needed
# All variables are optional - defaults shown below

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
TTS_HOST=0.0.0.0
TTS_PORT=8101
TTS_RELOAD=false
TTS_WORKERS=1

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
TTS_LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins, or "*" for all
TTS_CORS_ORIGINS=*
TTS_CORS_ALLOW_CREDENTIALS=true

# -----------------------------------------------------------------------------
# Model Configuration
# -----------------------------------------------------------------------------
# Hugging Face model repository
TTS_MODEL_REPO_ID=hexgrad/Kokoro-82M

# Language code: a=American English
TTS_MODEL_LANG_CODE=a

# Default voice for TTS
# Available: af_heart, af_bella, af_nicole, af_sarah, af_sky,
#            am_michael, am_adam, am_echo
TTS_DEFAULT_VOICE=af_heart

# -----------------------------------------------------------------------------
# Device Configuration
# -----------------------------------------------------------------------------
# Options: auto, cuda, mps, cpu
# - auto: Automatically detect best available device
# - cuda: Force NVIDIA GPU (requires CUDA)
# - mps: Force Apple Silicon GPU (macOS only, native run only - not in Docker)
# - cpu: Force CPU inference
TTS_DEVICE=auto

# -----------------------------------------------------------------------------
# HuggingFace Configuration
# -----------------------------------------------------------------------------
# Optional: HuggingFace API token for private models or higher rate limits
# Get your token at: https://huggingface.co/settings/tokens
# HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxx
